package com.roger;
import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
	// write your code here
        /*Scanner scanner = new Scanner(System.in);
        System.out.println("Enter your year of birth: ");

        boolean hasNextInt = scanner.hasNextInt(); //ask user for their input and ensures that it is an int, returns true or false

        if(hasNextInt){
            int yearOfBirth = scanner.nextInt(); // grabs entry from console and save it as an int
            scanner.nextLine(); // handle next line character (enter key)

            System.out.println("Enter your name: ");
            String name = scanner.nextLine(); // grabs entry from the console
            int age = 2021 - yearOfBirth;

            if(age >= 0 && age <= 100) {
                System.out.println("Your name is " + name + ", and you are " + age + " years old.");
            } else {
                System.out.println("Invalid Year of Birth.");
            }
        } else {
            System.out.println("Unable to parse uear of birth.");
        }

        scanner.close();*/
        ReadingUser
}
